[package]
name = "u-nesting-core"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
readme = "../../README.md"
description = "Core traits and abstractions for U-Nesting spatial optimization engine"
keywords = ["nesting", "bin-packing", "optimization", "geometry"]
categories = ["algorithms", "mathematics"]

[dependencies]
serde = { workspace = true, optional = true }
thiserror.workspace = true
rayon = { workspace = true, optional = true }
rand.workspace = true
log.workspace = true
# Wall-clock timing that works on both native (re-exports std::time) and wasm
# (performance.now()). Restores time_limit_ms termination on wasm.
web-time = "1"
u-metaheur = { version = "0.2", path = "../../../../algorithms/u-metaheur" }
u-numflow = { version = "0.2", path = "../../../../foundation/u-numflow" }
u-geometry = { version = "0.1", path = "../../../../algorithms/u-geometry" }

[dev-dependencies]
proptest.workspace = true
approx.workspace = true

[features]
default = []
serde = ["dep:serde", "u-geometry/serde"]
parallel = ["dep:rayon"]

[target.'cfg(target_arch = "wasm32")'.dependencies]
getrandom = { version = "0.3", features = ["wasm_js"] }
# Activate js feature for getrandom 0.2 used by transitive deps (e.g. i_overlay ecosystem)
getrandom_02 = { package = "getrandom", version = "0.2", features = ["js"] }
